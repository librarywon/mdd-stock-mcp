.venv/
.git/
tests/
__pycache__/
.pytest_cache/
*.egg-info/
dist/
.mypy_cache/
.ruff_cache/
CONTRIBUTING.md
SECURITY.md
# README.md NOT excluded: pyproject.toml references it via `readme = "README.md"`,
# so it must be present in the build context for `uv pip install` to succeed.
